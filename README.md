# Send-ERC20-Token
# Send ERC20 Token on Base

This example shows how to transfer ERC20 tokens.

## Install
npm install ethers

## Run
node sendToken.js

## Notes
- Requires token contract address
- Ensure sufficient token balance
